// import { create } from "zustand";
// import { checkImage } from "../../utils/imageupload";

// interface profileSlice {
//   profile: File | string;
//   setProfile: (profile: File | string) => void;
// }

// export const useProfileSlice = create<profileSlice>((set) => ({
//   profile: "",
//   setProfile: (profile) => set({ profile: checkImage(profile) }),
// }));
